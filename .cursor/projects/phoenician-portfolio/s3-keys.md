# S3 key templates — Portfolio (v4)

```
books/{book}/inputs/{selection,universe,stage0_ticker_meta}.json
books/{book}/allocations/{current,history/{ts}}.json
books/{book}/state/{performance,price_archive,exec…,cost_ledger,history_log}.json
inputs/earnings/…
inputs/capiq/companies/…
custom/{book,selection,performance,seed,research,manual_trades}.json
state/{…}
```
