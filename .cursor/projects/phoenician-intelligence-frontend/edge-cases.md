# Edge cases — PI FE (v4)

- Clear all `ph_*` on load/logout
- Block CapIQ submit if `screeningReportStatus===GENERATING`
- Cost embeds: Portfolio `/cost`; Screen `?section=costs&embed=1`
- Readonly CapIQ: `?embed=1&mode=readonly`
