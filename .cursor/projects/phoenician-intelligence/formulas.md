# Formulas — PI Python

See `tiny-logic.md` § Formulas + P/V gate details in `ue_dcf_main.py`.

Key: `pv_ratio`; gate band `[0.30, 3.00]`; edit caps 300bps / retry min 50bps; Trustpilot header similarity; Jaccard match thresholds.
