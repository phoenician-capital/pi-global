# Call architecture — pi-global-app

This Amplify SvelteKit site is the **ecosystem command center**.

| Kind | Present? |
|------|----------|
| Product runtime calls | **No** — no LLMs, CapIQ, or portal APIs |
| Data source | Static model in `src/lib/ecosystem/*` distilled from `.cursor/` KB |

UI consumes confirmed edges from linkage / call-architecture / project docs. Conceptual twins and forks are labeled. See live map for visualization.
