# Invariants — Factsheet-Automation

- Marketing collateral ≠ live book truth (portfolio engine / StrategyBook win).
- Fee copy: 1% / 20% over 6% hurdle — must match `fund.ts` if changing.
- Reweight uses `weight×(priceNow/anchor)` — see `tiny-logic.md`.
- No LLM; no write-back to CapIQ / PI / portal.
