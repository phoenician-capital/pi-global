# Env catalog — Factsheet-Automation

Essentially **none** for production (pure static client). Dev may use local Vite plugin config (`vite.sp500Api.ts`) only. No server secrets belong in this bundle.
