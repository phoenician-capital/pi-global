# API surface — Factsheet-Automation

**No production API.** Vite-only SPA.

Optional local Vite plugin `vite.sp500Api.ts` for S&P comparison data during dev — not a deployed backend. Holdings book loaded client-side (`useHoldingsBook`). Export = client PDF/Excel generation.
