# AI — investor-portal-mobile

**None on-device.** All AI (statement rename/segment/etc.) is admin/backend OpenAI — see portal `ai-prompting.md`. Mobile is auth + documents + KYC + push against portal API.
