# Env catalog — investor-portal-mobile (names only)

Typical Expo public API base / project IDs via `app.json` `extra` + EAS secrets. Nested fork may use different API host. Never commit Firebase / push credential JSON contents.

Point at portal API (same JWT/OTP/TOTP island as web portal) — `systems/auth-boundaries.md`.
